import { pageLoader, fullTextLoader, generateHTML } from './services';
import fullText from './fullText';
import outputForm from './outputForm';

export { pageLoader, fullTextLoader, generateHTML, fullText, outputForm };
